
#include <iostream>
using namespace std;
int main()
{
   

    // int age = 25;
    // double pi = 3.14;
    // char grade = 'A';
    // string name = "John"; // String (requires #include <string>)
    // bool isAdmin = true;  // Boolean (true or false)

    // Type Casting

    // Implicit Conversion
    // int x = 10;
    // float y = x; 

    //Explicit Conversion 
    // int x = 10;
    // float y = (float)x;             
    // float z = static_cast<float>(x); 

    // int a = 42;
    // double b = static_cast<double>(a); 
    // char c = 'A';
    // int d = static_cast<int>(c); // char to int (ASCII value)


    // Arithmatic Operations

    // int a = 10, b = 3;

    // cout << "Addition: " << a + b << endl;          // 13
    // cout << "Subtraction: " << a - b << endl;       // 7
    // cout << "Multiplication: " << a * b << endl;    // 30
    // cout << "Division: " << a / b << endl;          // 3 (integer division)
    // cout << "Modulus: " << a % b << endl;           // 1

    // // Increment and Decrement
    // int x = 5;
    // cout << "Pre-Increment: " << ++x << endl;       // 6
    // cout << "Post-Increment: " << x++ << endl;      // 6 (then x becomes 7)
    // cout << "Pre-Decrement: " << --x << endl;       // 6
    // cout << "Post-Decrement: " << x-- << endl;      // 6 (then x becomes 5)

    //Relational Operators


    // int a = 5, b = 3;

    // cout << "a == b: " << (a == b) << endl;  // 0 (false)
    // cout << "a != b: " << (a != b) << endl;  // 1 (true)
    // cout << "a > b: " << (a > b) << endl;    // 1 (true)
    // cout << "a < b: " << (a < b) << endl;    // 0 (false)
    // cout << "a >= b: " << (a >= b) << endl;  // 1 (true)
    // cout << "a <= b: " << (a <= b) << endl;  // 0 (false)


    //Logical Operators
    // bool a = true, b = false;
    // cout << "a && b: " << (a && b) << endl;   // 0 (false)
    // cout << "a || b: " << (a || b) << endl;   // 1 (true)
    // cout << "!a: " << (!a) << endl;           // 0 (false)

    // // Practical Example
    // int age = 25;
    // bool isAdult = (age >= 18 && age <= 60);
    // cout << "Is Adult: " << isAdult << endl;  // 1 (true)

    // bool hasLicense = false;
    // cout << "Can Drive: " << (isAdult && hasLicense) << endl; // 0 (false)


    //Character Storage
    // char c = 'a';
    // cout << "ASCII of 'a': " << (int)c << endl;
    // cout << "'a' + 1: " << (char)(c + 1) << endl;
    // cout << "'a' - 1: " << (char)(c - 1) <<endl;

    return 0;

}
