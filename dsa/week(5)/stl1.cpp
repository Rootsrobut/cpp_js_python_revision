#include <bits/stdc++.h>
using namespace std;

bool comp(pair<int, char> a, pair<int, char> b)
{
    return a.second > b.second;
}

int32_t main()
{

    // vector<int> v1(5);
    // for (int i = 0; i < 5; i++)
    // {
    //     cin >> v1[i];
    // }
    // cout<<v1.size()<<endl;
    // cout<<v1.capacity()<<endl;
    // v1.push_back(-1);
    // v1.pop_back();
    // v1.clear();
    // v1.back();
    // v1.front();




    // vector<int>::iterator it = v1.begin();
    // auto it=v1.begin();
    // for (; it != v1.end(); it++)
    // {
    //     cout << (*it) << endl;
    // }
    // for(auto ele : v1){
    // cout<<ele<<" ";
    // }
    // cout << v1.empty() << endl;



    // memset(v1 , 1 , sizeof(ar));
    // reverse(v1.begin() + 1 , v1.end() + 3 + 1);
    // v1.erase(v1.begin()+1,v1.begin() + 3 + 1);
    // sort(v1.begin(),v1.end(),greater<int>());
    // int n = sizeof(v1)/sizeof(v1[0]);
    // swap(v1[0],v1[1]);

    // for(int i = 0;i<5;i++) v1[i].resize(c);
    // for(int i = 0;i<5;i++) v1.push_back(vector<int>(c));


    // vector<int> v2(5, 1);
    // int row, col;
    // cin >> row >> col;
    // vector<vector<int>> v3(row, vector<int>(col));

    

    // vector<pair<int, char>> students;
    // pair<int , char> p;
    // pair<int , char> p(10,'A');
    // pair<int , char> p = {10,'A'};
    // pair<int , char> p = make_pair(10,'A')

    // cin >> num;
    // for (int i = 0; i < num; i++)
    // {
    //     cin >> roll_number >> section;
    //     students.push_back({roll_number, section});
    // }

    // sort(students.begin(), students.end(), comp);
    // for (int i = 0; i < n; i++)
    // {
    //     cout << students[i].first << " " << students[i].second << endl;
    // }

    return 0;
}